# FlakeSting Legacy Frontend

This directory preserves the audited v1 FlakeSting React frontend after the
2026-07-09 v1-to-v2 parity pass.

The active frontend now lives in `../frontend-v2/` and is the default Docker and
local development UI. Keep this legacy copy only while comparing behavior before
deleting old UI code.
