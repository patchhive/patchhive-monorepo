name: CodeQL

on:
  push:
    branches:
      - main
  pull_request:
  schedule:
    - cron: "17 7 * * 2"
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ubuntu-latest
    timeout-minutes: 120
    strategy:
      fail-fast: false
      matrix:
        include:
          - language: actions
            build-mode: none
          - language: javascript-typescript
            build-mode: none
          - language: rust
            build-mode: none

    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Set up Rust
        if: matrix.language == 'rust'
        uses: dtolnay/rust-toolchain@stable

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: ${{ matrix.language }}
          build-mode: ${{ matrix.build-mode }}

      - name: Analyze Rust
        if: matrix.language == 'rust'
        env:
          CODEQL_EXTRACTOR_RUST_CARGO_ALL_TARGETS: "true"
          CODEQL_EXTRACTOR_RUST_EXTRACT_DEPENDENCIES_AS_SOURCE: "true"
        uses: github/codeql-action/analyze@v4

      - name: Analyze
        if: matrix.language != 'rust'
        uses: github/codeql-action/analyze@v4
