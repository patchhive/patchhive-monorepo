# Keep default cargo commands aligned with standalone product repos. Local shared
# crate overrides are available opt-in through `.cargo/local-patches.toml` when
# actively developing a shared crate against one or more products.
[net]
git-fetch-with-cli = true
